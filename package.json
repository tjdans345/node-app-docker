{
  "name": "nodejs-docker-app",
  "version": "1.0.0",
  "description": "docker + nodejs + app",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "meteor",
  "license": "ISC"
}
